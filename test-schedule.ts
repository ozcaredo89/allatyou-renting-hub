import { generateLeasingSchedule } from './src/lib/leasingCascade'; const schedule = generateLeasingSchedule(1, 15000000, 1.5, 10000, 11000, 35000, '2026-07-20', new Set()); const last = schedule[schedule.length - 1]; console.log('Total cuotas:', schedule.length); console.log('Ultima cuota capital:', last.principal_expected); console.log('Interes ultima cuota:', last.interest_expected); const totalPrincipal = schedule.reduce((sum, r) = + r.principal_expected, 0); console.log('Total Capital Pagado (debe ser 15000000 exacto):', totalPrincipal); 
